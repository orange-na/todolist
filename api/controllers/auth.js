const register = (req, res) => {
  res.json("hello users!!");
};

const login = (req, res) => {
  res.json("hello users!!");
};

const logout = (req, res) => {
  res.json("hello users!!");
};

module.exports = { register, login, logout };
